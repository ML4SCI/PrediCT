#torch>=2.0.0

# Install GPU version of torch manually if you are on windows, on macOs it is okay, u can uncomment and directly run "python -m pip install -r requirements.txt "

SimpleITK>=2.2.0
nnunetv2>=2.2.0